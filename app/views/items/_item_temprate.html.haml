%li.item-list__box
  .item-list__box__parts
    =link_to item_path(item.id), class: 'item-list__box__link' do
      %figure.item-list__box__link__figure
        %figreption
          %span.item-list__box__name
            #{item.name}
        .item-list__box__main
          %span.item-list__box__main__price{"aria-label" => "Price"} ¥#{item.price}
          - item.images.first(1).each do |i|
            = image_tag i.image.to_s, class: "item-list__box__main__img"